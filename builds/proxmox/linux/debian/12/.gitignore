# Packer build artifacts
*.box
packer_cache/
crash.log
packer-manifest.json

# Variable files with secrets
*.auto.pkrvars.hcl
**/common.auto.pkrvars.hcl
variables.auto.pkrvars.hcl

# EXCEPT example files (these are safe to commit)
!*.auto.pkrvars.hcl.example
!common.auto.pkrvars.hcl.example
!variables.auto.pkrvars.hcl.example

# Build manifests (optional - remove if you want to track these)
**/manifests/*.json

# SSH keys (generated by sshkey plugin)
*.pem
*.pub
id_rsa*
id_ed25519*
id_ecdsa*

# Ansible logs
ansible.log
*.retry

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary files
*.tmp
tmp/
temp/
